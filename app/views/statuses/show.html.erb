<h1>/Status</h1>

<p>
  <b>Name</b><br>
  <%= @status.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', statuses_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_status_path(@status), :class => 'btn' %>
  <%= link_to 'Delete', status_path(@status), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
